{
  "name": "tiny-form-validation",
  "version": "1.1.1",
  "description": "Small utility to help form validation",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "yarn node --experimental-vm-modules $(yarn bin jest)",
    "postinstall": "yarn node --experimental-vm-modules $(yarn bin jest)"
  },
  "author": "Nicola Dal Pont",
  "license": "MIT",
  "dependencies": {
    "lodash.get": "^4.4.2",
    "lodash.set": "^4.3.2"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
